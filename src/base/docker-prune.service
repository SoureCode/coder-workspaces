[Unit]
Description=Aggressive docker prune on shutdown
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/bin/true
ExecStop=/usr/bin/docker system prune -af --volumes
TimeoutStopSec=120

[Install]
WantedBy=multi-user.target
